//  export all the files in this folder 
//  this is similar to what sequelize does for us 
//  so we import ALL models here and then export them
module.exports = {
    //  from the same folder 
    Product: require("./product.js")

}